fastapi_project/
│── app/
│   ├── routers/
│   │   ├── users.py           # User management APIs (register, login, get users)
│   │   ├── transactions.py    # Transactions API (get transactions, add transactions)
│   │   ├── purchases.py       # Purchase API (list purchases, get purchase by ID)
│   │   ├── auth.py            # Authentication (JWT-based login, token handling)
│   │   ├── __init__.py        # Makes this a Python package
│   │
│   ├── models/
│   │   ├── user_model.py      # User table schema
│   │   ├── transaction_model.py # Transactions table schema
│   │   ├── purchase_model.py  # Purchases table schema
│   │   ├── __init__.py        # Makes this a package
│   │
│   ├── database.py            # MySQL database connection setup
│   ├── main.py                # FastAPI entry point (includes routers)
│   ├── config.py              # Configuration settings (DB credentials, JWT secret)
│   ├── utils.py               # Utility functions (hashing passwords, token validation)
│
│── requirements.txt           # Required Python packages
│── Dockerfile                 # Docker container setup
│── docker-compose.yml         # Docker Compose for running FastAPI + MySQL
│── .env                       # Environment variables (DB credentials, JWT secret)
│── README.md                  # Project documentation


//Use the bellow cmd to create the directory and file


mkdir -p fastapi_project/app/routers
mkdir -p fastapi_project/app/models

# Create Python files
touch fastapi_project/app/main.py
touch fastapi_project/app/database.py
touch fastapi_project/app/config.py
touch fastapi_project/app/utils.py

# Create routers
touch fastapi_project/app/routers/__init__.py
touch fastapi_project/app/routers/users.py
touch fastapi_project/app/routers/transactions.py
touch fastapi_project/app/routers/purchases.py
touch fastapi_project/app/routers/auth.py

# Create models
touch fastapi_project/app/models/__init__.py
touch fastapi_project/app/models/user_model.py
touch fastapi_project/app/models/transaction_model.py
touch fastapi_project/app/models/purchase_model.py

# Create other necessary files
touch fastapi_project/requirements.txt
touch fastapi_project/Dockerfile
touch fastapi_project/docker-compose.yml
touch fastapi_project/.env
touch fastapi_project/README.md